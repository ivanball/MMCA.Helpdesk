{
  "$schema": "http://json.schemastore.org/template",
  "author": "Ivan Ball-llovera",
  "classifications": [ "DDD", "CQRS", "Clean Architecture", "Modular Monolith", "MMCA" ],
  "identity": "MMCA.Templates.Module",
  "name": "MMCA business module (five layers + tests + migrations)",
  "shortName": "mmca-module",
  "description": "A new business module for an existing MMCA solution: the Shared, Domain, Application, Infrastructure, and API projects, an aggregate root with invariants and domain events, the IModule registration, a REST controller, domain and application test projects, and a migrations project for the module's own database. Shape it with --flat (no child collection), --no-status (no status axis), --no-description (no description property), --no-owner (no requester/owner property), and --child (rename the child entity); name its vocabulary with --title (the aggregate's main text property) and --event-verb (the creation integration event's verb). Match it to the solution's relational engine with --database sqlserver|sqlite, which must be the engine the surrounding app was generated with. Run it from your solution root.",
  "tags": {
    "language": "C#",
    "type": "item"
  },
  "sourceName": "Tickets",
  "defaultName": "Sample",
  "preferNameDirectory": false,

  "symbols": {
    "app": {
      "type": "parameter",
      "datatype": "text",
      "isRequired": true,
      "replaces": "MMCA.Helpdesk",
      "fileRename": "MMCA.Helpdesk",
      "description": "The solution / root namespace this module belongs to, for example Contoso.Support."
    },
    "aggregate": {
      "type": "parameter",
      "datatype": "text",
      "isRequired": true,
      "replaces": "Ticket",
      "fileRename": "Ticket",
      "description": "The module's aggregate root, singular PascalCase, for example Invoice."
    },
    "aggregateLower": {
      "type": "derived",
      "valueSource": "aggregate",
      "valueTransform": "lowerCase",
      "replaces": "ticket",
      "description": "Lower-case aggregate name: local variables, route segments, and resource-string prose."
    },
    "title": {
      "type": "parameter",
      "datatype": "text",
      "defaultValue": "Title",
      "replaces": "Title",
      "description": "Name of the aggregate's main text property, singular PascalCase (for example Name, Subject, CustomerName). Everything that names it moves with it: the domain property and its invariant, the error codes (<aggregate>.<Title>.Empty and .TooLong), the <Title>MaxLength constant, the DTO and request properties, the EF column, the FluentValidation rule, and the domain tests. No fileRename: no file in the seed is named after it."
    },
    "titleCamel": {
      "type": "derived",
      "valueSource": "title",
      "valueTransform": "firstLowerCase",
      "replaces": "title",
      "description": "Camel-case form of the main text property: factory and method parameters, private fields, named arguments, and nameof() targets. firstLowerCase (not lowerCase) is what keeps a multi-word value a valid camel-case identifier: CustomerName -> customerName, not customername. The price is that a plain substring replacement also reaches ENGLISH PROSE, so the two shipped error messages read \"cannot be empty\" against the camel-case spelling for a multi-word value. Deliberate trade-off: identifiers have to compile, and the two strings are a one-line edit in the module's error resources after generating."
    },
    "eventVerb": {
      "type": "parameter",
      "datatype": "text",
      "defaultValue": "Opened",
      "replaces": "Opened",
      "fileRename": "Opened",
      "description": "Verb naming the creation integration event, past tense PascalCase (for example Created, Placed, Booked). The generated event is <aggregate><EventVerb>IntegrationEvent and its consumer <aggregate><EventVerb>Handler, so --aggregate Shipment --event-verb Dispatched produces ShipmentDispatchedIntegrationEvent and ShipmentDispatchedHandler. The id is database-generated, so this event is published AFTER the commit by the create handler rather than raised as a domain event by the factory."
    },
    "eventVerbLower": {
      "type": "derived",
      "valueSource": "eventVerb",
      "valueTransform": "lowerCase",
      "replaces": "opened",
      "description": "Lower-case verb: the prose that has to follow the event name rather than sit beside it, in the event's XML doc and the consumer's log message."
    },
    "child": {
      "type": "parameter",
      "datatype": "text",
      "defaultValue": "Comment",
      "replaces": "Comment",
      "fileRename": "Comment",
      "description": "Name of the aggregate's child entity, singular PascalCase. The generated type is <aggregate><child>, so --aggregate Shipment --child Line produces ShipmentLine with AddLine / EditLine / RemoveLine slices and /lines routes. IGNORED when --flat is passed, which drops the child collection entirely."
    },
    "childLower": {
      "type": "derived",
      "valueSource": "child",
      "valueTransform": "lowerCase",
      "replaces": "comment",
      "description": "Lower-case child name: local variables, parameter names, and prose."
    },
    "childPlural": {
      "type": "derived",
      "valueSource": "child",
      "valueTransform": "toPlural",
      "replaces": "Comments",
      "description": "Plural child name: the navigation collection, the DbSet, and the use-case folder names. Derived by an English pluralizer (Line -> Lines, Entry -> Entries, Box -> Boxes); rename by hand if your noun is irregular."
    },
    "childPluralLower": {
      "type": "derived",
      "valueSource": "child",
      "valueTransform": "toPluralLower",
      "replaces": "comments",
      "description": "Lower-case plural child name: the REST route segment and prose."
    },
    "flat": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Generate the module WITHOUT a child collection: no child entity, DTO, requests, mapper, EF configuration, Add/Edit/Remove slices, controller endpoints, identifier alias, or tests. Use it when your aggregate owns no growable children. Makes --child irrelevant."
    },
    "noStatus": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Generate the module WITHOUT a status axis: no status enum, no ChangeStatus slice, request, or endpoint, no Status property, and no status invariant or tests. Use it when your aggregate has no lifecycle state."
    },
    "noDescription": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Generate the module WITHOUT the aggregate's long-text property: no Description property or invariant, no <Description>MaxLength constant, no DTO, request, or command field, no validator rule, no EF max-length configuration, no error-resource entries, and no tests for it. Use it when the aggregate's main text property is all the text it needs."
    },
    "noOwner": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "false",
      "description": "Generate the module WITHOUT the aggregate's owning-user property: no RequesterUserId property, no create-request field or validator rule, no member on the creation integration event, no EF index, and no tests for it. Use it when the aggregate is not owned by a single user, or until you add an Identity module to resolve one."
    },
    "moduleLower": {
      "type": "derived",
      "valueSource": "name",
      "valueTransform": "lowerCase",
      "replaces": "tickets",
      "description": "Lower-case module name: routes and the data-source key."
    },
    "moduleUpper": {
      "type": "derived",
      "valueSource": "name",
      "valueTransform": "upperCase",
      "replaces": "TICKETS",
      "description": "Upper-case module name: the design-time connection-string environment variable."
    },
    "appShortName": {
      "type": "derived",
      "valueSource": "app",
      "valueTransform": "toLastSegment",
      "replaces": "Helpdesk",
      "description": "Last dotted segment of the app name, used by the design-time factory's database name."
    },
    "appShortNameLower": {
      "type": "derived",
      "valueSource": "app",
      "valueTransform": "toLastSegmentLower",
      "replaces": "helpdesk",
      "description": "Lower-case short name. It exists for the sqlite database option alone: a SQLite data source is a FILE, and the design-time factory's fallback names it <app-short>_<module>.db, which is the same per-module file build/add-module.ps1 writes into the host's DataSources section. Nothing else in a module carries the lower-case app name."
    },
    "appShortNameUpper": {
      "type": "derived",
      "valueSource": "app",
      "valueTransform": "toLastSegmentUpper",
      "replaces": "HELPDESK",
      "description": "Upper-case short name: the design-time connection-string environment variable."
    },

    "database": {
      "type": "parameter",
      "datatype": "choice",
      "defaultValue": "sqlserver",
      "choices": [
        {
          "choice": "sqlserver",
          "description": "SQL Server. Entity configurations inherit EntityTypeConfigurationSQLServer, the migrations project is named .Migrations.SqlServer.<Module> and targets Microsoft.EntityFrameworkCore.SqlServer, and the design-time factory builds a SQLServerDbContext."
        },
        {
          "choice": "sqlite",
          "description": "SQLite. Entity configurations inherit EntityTypeConfigurationSqlite, the migrations project is named .Migrations.Sqlite.<Module> and targets Microsoft.EntityFrameworkCore.Sqlite, and the design-time factory builds a SqliteDbContext over a database file."
        }
      ],
      "description": "Relational engine for this module. It MUST match the engine the surrounding solution was generated with (dotnet new mmca-app --database), because the engine decides which configuration base the module's EF configurations inherit and which provider package its migrations project references. build/add-module.ps1 detects the app's engine and passes this for you."
    },
    "useSqlite": {
      "type": "computed",
      "value": "(database == \"sqlite\")"
    },
    "engineName": {
      "type": "derived",
      "valueSource": "database",
      "valueTransform": "toEngineName",
      "replaces": "SqlServer",
      "fileRename": "SqlServer",
      "description": "Pascal spelling of the engine (SqlServer / Sqlite). It renames the migrations project (folder, csproj, namespace, assembly), the engine provider package id, and the design-time helper call DesignTimeDbContextHelper.CreateSqlServer -> CreateSqlite. DERIVED from --database; there is no flag of its own. Declared identically in mmca-app: the two templates are staged separately, so neither inherits the other's symbols."
    },
    "engineNameUpper": {
      "type": "derived",
      "valueSource": "database",
      "valueTransform": "toEngineNameUpper",
      "replaces": "SQLServer",
      "fileRename": "SQLServer",
      "description": "The framework's OTHER spelling of the same engine (SQLServer / Sqlite), which is the one its public types and settings use. It renames the design-time factory (file and class), SQLServerDbContext -> SqliteDbContext, EntityTypeConfigurationSQLServer -> EntityTypeConfigurationSqlite, and the SQLServerConnectionString / SQLServerMigrationsAssembly settings properties. Two symbols rather than one because the two spellings are both real: renaming either alone leaves half the module pointing at the other engine."
    }
  },

  "forms": {
    "lowerCase": { "identifier": "lowerCase" },
    "upperCase": { "identifier": "upperCase" },
    "firstLowerCase": { "identifier": "firstLowerCase" },
    "toLastSegment": { "identifier": "replace", "pattern": "^.*\\.", "replacement": "" },
    "toLastSegmentLower": { "identifier": "chain", "steps": [ "toLastSegment", "lowerCase" ] },
    "toLastSegmentUpper": { "identifier": "chain", "steps": [ "toLastSegment", "upperCase" ] },
    "pluralConsonantY": { "identifier": "replace", "pattern": "([^aeiouAEIOU])[yY]$", "replacement": "$1ie" },
    "pluralSibilant": { "identifier": "replace", "pattern": "([sxzSXZ]|[cC][hH]|[sS][hH])$", "replacement": "$1e" },
    "pluralS": { "identifier": "replace", "pattern": "$", "replacement": "s" },
    "toPlural": { "identifier": "chain", "steps": [ "pluralConsonantY", "pluralSibilant", "pluralS" ] },
    "toPluralLower": { "identifier": "chain", "steps": [ "toPlural", "lowerCase" ] },

    "engineSqlServerPascal": { "identifier": "replace", "pattern": "^sqlserver$", "replacement": "SqlServer" },
    "engineSqlitePascal": { "identifier": "replace", "pattern": "^sqlite$", "replacement": "Sqlite" },
    "toEngineName": { "identifier": "chain", "steps": [ "engineSqlServerPascal", "engineSqlitePascal" ] },
    "engineSqlServerUpper": { "identifier": "replace", "pattern": "^sqlserver$", "replacement": "SQLServer" },
    "toEngineNameUpper": { "identifier": "chain", "steps": [ "engineSqlServerUpper", "engineSqlitePascal" ] }
  },

  "sources": [
    {
      "modifiers": [
        {
          "condition": "(flat)",
          "exclude": [
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Domain/Tickets/TicketComment.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Shared/Tickets/TicketCommentDTO.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Shared/Tickets/AddCommentRequest.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Shared/Tickets/EditCommentRequest.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Application/Tickets/DTOs/TicketCommentDTOMapper.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Application/Tickets/UseCases/AddComment/**",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Application/Tickets/UseCases/EditComment/**",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Application/Tickets/UseCases/RemoveComment/**",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Infrastructure/Persistence/EntityConfiguration/TicketCommentConfiguration.cs"
          ]
        },
        {
          "condition": "(noStatus)",
          "exclude": [
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Shared/Tickets/TicketStatus.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Shared/Tickets/ChangeTicketStatusRequest.cs",
            "Source/Modules/Tickets/MMCA.Helpdesk.Tickets.Application/Tickets/UseCases/ChangeStatus/**"
          ]
        }
      ],
      "exclude": [
        ".template.config/**",
        "**/bin/**",
        "**/obj/**"
      ]
    }
  ],

  "postActions": [
    {
      "description": "Wire the module into the solution.",
      "manualInstructions": [
        {
          "comment": "The sqlite branch. dotnet new picks the FIRST entry whose condition holds, so this one has to sit above the unconditioned SQL Server text below. The two differ by more than a spelling: there is no container resource to declare, the settings keys are the framework's other engine spelling, and the first migration stops being optional (a SQLite source that names a migrations assembly is MIGRATED at startup rather than created outright, so an empty migrations assembly leaves an empty database).",
          "condition": "(useSqlite)",
          "text": "FIRST, IF YOUR SOLUTION CAME FROM dotnet new mmca-app: it already ships a script that does everything below, and creates the first migration. Undo this run (delete Source/Modules/<Module>, Tests/Modules/<Module> and Source/Hosting/<App>.Migrations.Sqlite.<Module>) and use it instead, from your solution root:\n     pwsh build/add-module.ps1 -Name <Module> -Aggregate <Aggregate>\n   It detects this solution's engine and passes it back to this template, and it takes the same shape options (-Child, -Flat, -NoStatus, -NoOwner, -NoDescription, -Title, -EventVerb). Run it with -? for the full list. The steps below are the fallback for a hand-built solution, and the reference for what the script does.\n\ndotnet new cannot make these edits for you. Until they are done the module is invisible to the host and to the fitness rules. Do them in order: steps 1 and 2 are what make it compile.\n\n1. SOLUTION: add the eight new projects.\n     dotnet sln <YourApp>.slnx add Source/Modules/<Module>/*/*.csproj Tests/Modules/<Module>/*/*.csproj Source/Hosting/<App>.Migrations.Sqlite.<Module>/*.csproj\n   PowerShell (no glob expansion):\n     dotnet sln <YourApp>.slnx add (Get-ChildItem Source\\Modules\\<Module>\\*\\*.csproj, Tests\\Modules\\<Module>\\*\\*.csproj, Source\\Hosting\\<App>.Migrations.Sqlite.<Module>\\*.csproj).FullName\n\n2. PROJECT REFERENCES: nothing references the new projects yet, so their namespaces do not resolve.\n   - <App>.Web.csproj: add a ProjectReference to <App>.<Module>.API and to <App>.Migrations.Sqlite.<Module>.\n   - <App>.Architecture.Tests.csproj: add a ProjectReference to ALL FIVE layer projects (Domain, Application, Infrastructure, Shared, API). The map in step 4 names a type from each.\n\n3. IDENTIFIER ALIAS: in Directory.Build.props, copy the existing <Compile Include ... Link> block and point it at Source/Modules/<Module>/<App>.<Module>.Shared/<App>.<Module>.GlobalUsings.IdentifierType.cs, with Condition=\"'$(MSBuildProjectName)' != '<App>.<Module>.Shared'\". Without it the identifier alias is invisible outside its own project.\n\n4. ARCHITECTURE MAP: add five lines to your *ArchitectureMap.cs, one per layer:\n     Module(\"<Module>\", Layer.Domain, typeof(<App>.<Module>.Domain.<Module>.<Aggregate>).Assembly),\n     Module(\"<Module>\", Layer.Application, typeof(<App>.<Module>.Application.ClassReference).Assembly),\n     Module(\"<Module>\", Layer.Infrastructure, typeof(<App>.<Module>.Infrastructure.AssemblyReference).Assembly),\n     Module(\"<Module>\", Layer.Shared, typeof(<App>.<Module>.Shared.<Module>.<Aggregate>DTO).Assembly),\n     Module(\"<Module>\", Layer.Api, typeof(<App>.<Module>.API.Controllers.<Module>Controller).Assembly),\n   A module missing from the map is silently NOT covered by the layering and isolation rules.\n\n5. HOST: register the module's error-code translations next to the existing ones in your Web host's Program.cs:\n     services.AddErrorResources<<Module>ErrorResources>();\n   ModuleLoader discovers the IModule itself, so there is nothing else to register.\n\n6. DATABASE: give the module its own database FILE. Every module database carries its own dbo.OutboxMessages/InboxMessages tables, so two modules migrated into one file collide on them, and per-module databases are what make extracting the module a host-only change later (ADR-006).\n   - AppHost Program.cs, IF this solution has one: SQLite is an in-process file, so there is no resource to declare. Chain a second data source onto the web project, ABOVE the existing one: every data-source call also rewrites the top-level connection string, so the LAST one decides which module is the Default source, and that has to stay the first module (its migrations are the ones scaffolded with the Default-source-only tables in them).\n       .WithSqliteDataSource(\"<Module>\", Path.Combine(builder.AppHostDirectory, \"<app-short>_<module>.db\"))\n   - <App>.Web appsettings.json: enable the module under Modules and give it its own DataSources entry beside the existing one:\n       \"Modules\": { ..., \"<Module>\": { \"Enabled\": true } },\n       \"DataSources\": {\n         \"<FirstModule>\": { \"SqliteConnectionString\": \"Data Source=<app-short>.db\", \"SqliteMigrationsAssembly\": \"<App>.Migrations.Sqlite.<FirstModule>\" },\n         \"<Module>\": { \"SqliteConnectionString\": \"Data Source=<app-short>_<module>.db\", \"SqliteMigrationsAssembly\": \"<App>.Migrations.Sqlite.<Module>\" }\n       },\n       \"Outbox\": { \"DatabaseName\": \"<FirstModule>\" }\n     The Outbox section is top level, beside ConnectionStrings. IEventBus writes handler-published integration events to ONE configured outbox source per host; it defaults to Default, and both WithSqliteDataSource and the top-level ConnectionStrings entry name that Default file, so one module always collapses onto it. Naming the source explicitly is what stops the outbox from moving to another module's file the day you reorder those calls.\n     Keep the top-level ConnectionStrings SqliteConnectionString (it is the Default fallback used by startup validation and the health checks), and if it carries a SqliteMigrationsAssembly line, DELETE that one: a top-level pin naming one module's assembly fails startup with \"conflicting SqliteMigrationsAssembly values\" once the other module routes its own source.\n\n7. MIGRATION, BEFORE THE FIRST RUN: create the module's first migration (the project ships Migrations/.editorconfig, which keeps analyzer enforcement off the generated migration files).\n     dotnet ef migrations add InitialCreate --project Source/Hosting/<App>.Migrations.Sqlite.<Module> --startup-project Source/Hosting/<App>.Migrations.Sqlite.<Module> --context SqliteDbContext\n   This one is REQUIRED rather than something to get to later. A SQLite data source that names a SqliteMigrationsAssembly is MIGRATED at startup instead of being created outright, so a module whose migrations assembly is still empty starts against an empty database and every one of its queries fails on a missing table."
        },
        {
          "text": "FIRST, IF YOUR SOLUTION CAME FROM dotnet new mmca-app: it already ships a script that does everything below, and creates the first migration. Undo this run (delete Source/Modules/<Module>, Tests/Modules/<Module> and Source/Hosting/<App>.Migrations.SqlServer.<Module>) and use it instead, from your solution root:\n     pwsh build/add-module.ps1 -Name <Module> -Aggregate <Aggregate>\n   It takes the same shape options as this template (-Child, -Flat, -NoStatus, -NoOwner, -NoDescription, -Title, -EventVerb) and passes them through. Run it with -? for the full list. The steps below are the fallback for a hand-built solution, and the reference for what the script does.\n\ndotnet new cannot make these edits for you. Until they are done the module is invisible to the host and to the fitness rules. Do them in order: steps 1 and 2 are what make it compile.\n\n1. SOLUTION: add the eight new projects.\n     dotnet sln <YourApp>.slnx add Source/Modules/<Module>/*/*.csproj Tests/Modules/<Module>/*/*.csproj Source/Hosting/<App>.Migrations.SqlServer.<Module>/*.csproj\n   PowerShell (no glob expansion):\n     dotnet sln <YourApp>.slnx add (Get-ChildItem Source\\Modules\\<Module>\\*\\*.csproj, Tests\\Modules\\<Module>\\*\\*.csproj, Source\\Hosting\\<App>.Migrations.SqlServer.<Module>\\*.csproj).FullName\n\n2. PROJECT REFERENCES: nothing references the new projects yet, so their namespaces do not resolve.\n   - <App>.Web.csproj: add a ProjectReference to <App>.<Module>.API and to <App>.Migrations.SqlServer.<Module>.\n   - <App>.Architecture.Tests.csproj: add a ProjectReference to ALL FIVE layer projects (Domain, Application, Infrastructure, Shared, API). The map in step 4 names a type from each.\n\n3. IDENTIFIER ALIAS: in Directory.Build.props, copy the existing <Compile Include ... Link> block and point it at Source/Modules/<Module>/<App>.<Module>.Shared/<App>.<Module>.GlobalUsings.IdentifierType.cs, with Condition=\"'$(MSBuildProjectName)' != '<App>.<Module>.Shared'\". Without it the identifier alias is invisible outside its own project.\n\n4. ARCHITECTURE MAP: add five lines to your *ArchitectureMap.cs, one per layer:\n     Module(\"<Module>\", Layer.Domain, typeof(<App>.<Module>.Domain.<Module>.<Aggregate>).Assembly),\n     Module(\"<Module>\", Layer.Application, typeof(<App>.<Module>.Application.ClassReference).Assembly),\n     Module(\"<Module>\", Layer.Infrastructure, typeof(<App>.<Module>.Infrastructure.AssemblyReference).Assembly),\n     Module(\"<Module>\", Layer.Shared, typeof(<App>.<Module>.Shared.<Module>.<Aggregate>DTO).Assembly),\n     Module(\"<Module>\", Layer.Api, typeof(<App>.<Module>.API.Controllers.<Module>Controller).Assembly),\n   A module missing from the map is silently NOT covered by the layering and isolation rules.\n\n5. HOST: register the module's error-code translations next to the existing ones in your Web host's Program.cs:\n     services.AddErrorResources<<Module>ErrorResources>();\n   ModuleLoader discovers the IModule itself, so there is nothing else to register.\n\n6. DATABASE: give the module its own database. Every module database carries its own dbo.OutboxMessages/InboxMessages tables, so two modules migrated into one database collide on them, and per-module databases are what make extracting the module a host-only change later (ADR-006).\n   - AppHost Program.cs: add a database beside the existing one and hand it to the web project:\n       var <module>Db = sql.AddDatabase(\"<app-short>-<module>\", \"<AppShort>_<Module>\");\n       // on the web project, chained ABOVE the existing call: every data-source call also rewrites the top-level connection string, so the LAST one decides which module is the Default source, and that has to stay the first module (its migrations are the ones scaffolded with the Default-source-only tables in them).\n       .WithSQLServerDataSource(<module>Db, \"<Module>\")\n   - <App>.Web appsettings.json: enable the module under Modules, give EVERY module (the first one too) a DataSources entry, and pin the outbox to ONE of them:\n       \"Modules\": { ..., \"<Module>\": { \"Enabled\": true } },\n       \"DataSources\": {\n         \"<FirstModule>\": { \"SQLServerConnectionString\": \"Server=localhost;Database=<AppShort>_<FirstModule>;...\", \"SQLServerMigrationsAssembly\": \"<App>.Migrations.SqlServer.<FirstModule>\" },\n         \"<Module>\": { \"SQLServerConnectionString\": \"Server=localhost;Database=<AppShort>_<Module>;...\", \"SQLServerMigrationsAssembly\": \"<App>.Migrations.SqlServer.<Module>\" }\n       },\n       \"Outbox\": { \"DatabaseName\": \"<FirstModule>\" }\n     The Outbox section is top level, beside ConnectionStrings. IEventBus writes handler-published integration events to ONE configured outbox source per host; it defaults to Default, and under Aspire the Default connection string is whichever module's WithSQLServerDataSource call ran last. Naming the source explicitly is what stops the outbox from moving to another module's database the day you reorder those calls.\n     Then DELETE the SQLServerMigrationsAssembly line from the top-level ConnectionStrings section (keep the connection string itself: it is the Default fallback used by validation and health checks). Under Aspire every WithSQLServerDataSource call also rewrites the top-level connection string and the last one wins, so one module always collapses onto the Default source; a top-level assembly pin naming the other module's assembly then fails startup with \"conflicting SQLServerMigrationsAssembly values\".\n\n7. MIGRATION: create the module's first migration (the project ships Migrations/.editorconfig, which keeps analyzer enforcement off the generated migration files).\n     dotnet ef migrations add InitialCreate --project Source/Hosting/<App>.Migrations.SqlServer.<Module> --startup-project Source/Hosting/<App>.Migrations.SqlServer.<Module> --context SQLServerDbContext"
        }
      ],
      "actionId": "AC1156F7-BB77-4DB8-B28F-24EEBCCA1E5C",
      "continueOnError": true
    }
  ]
}

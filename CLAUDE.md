# CLAUDE.md

@AGENTS.md

The repo guidance lives in `AGENTS.md`, the vendor-neutral instruction file every coding agent reads; the import above gives Claude Code the same text. Put Claude-specific notes, if any, below this line so `AGENTS.md` stays tool-agnostic.
